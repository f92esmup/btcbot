En este punto vamos a estudiar que server.py funcione como esperamos. Lo que debe hacer, unica y  exclusivamente debe servir como endpoint, carga un modelo y ofrece predicciones. Nada mas, todas los secretos se manejan con google cloud secrets (al igaul que todo el proyecto), el modelo se obtiene de gcs tambien (unicamente de gcs). La idea es que funciona localmente pero usando todos los servisios de google cloud. Las variables de entorno que necesite obligatoriamente, como el path del modelo, se incluyen como argumentos. También quiero eliminar la variables de enorno ENV ENVIRONMENT="production", ya que siempre debe actuar como si estuviera en produccion. Si no me equivoc, con esta configuración no importa si lo ejecuto en la nube o en local funcionará de la misma forma. Además, la configuración se sigue centtralizando en src/utils/config.py